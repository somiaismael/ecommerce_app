import 'package:flutter/material.dart';

class AuthDonthaveaccount extends StatelessWidget {
  const AuthDonthaveaccount({super.key});

  @override
  Widget build(BuildContext context) {
    return  Text(
                            "Don't have an account?",
                            style: TextStyle(
                              color: Colors.black,
                            ),
                          );
  }
}